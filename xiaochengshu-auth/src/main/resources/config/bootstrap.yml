server:
  servlet:
    context-path: /auth
spring:
  application:
    name: auth
  cloud:
    nacos:
      config:
        server-addr: http://192.168.247.141:8848
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace: xiaochengshu
        file-extension: yaml
        refresh-enabled: true
      discovery:
        enabled: true
        group: DEFAULT_GROUP
        namespace: xiaochengshu
        server-addr: 192.168.247.141:8848